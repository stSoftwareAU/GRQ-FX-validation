Fixed the **markdownlint** failure (MD060 — `table-column-style`).

**Root cause:** `docs/archive/pr-summaries/pr-summary-43.md` lines 34 and 43 had table separator rows written as `|---|---|---|`, which lacks the spaces required by the compact column-style rule.

**Fix:** Changed both separators to `| --- | --- | --- |` (commit `804b356` on `milestone/audit-issues`).

No other files were modified. The markdownlint check should now pass.
